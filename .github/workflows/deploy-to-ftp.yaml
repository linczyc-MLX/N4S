name: Deploy to website.not-4.sale (FTPS)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: CI=false npm run build

      - name: Deploy to FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: home148422849.1and1-data.host
          username: acc780882701
          password: ${{ secrets.FTP_PASSWORD }}
          port: 990
          protocol: ftps
          local-dir: ./build/
          server-dir: /
```

4. **Click "Commit changes..."** (green button top right)

5. **In the popup, click "Commit changes"**

---

### Step 4: After Creating on GitHub

**In Claude Code, say:**
```
# Reset local commit since we created it on GitHub instead
cd ~/N4S-clone && git reset --hard HEAD~1

# Pull the workflow from GitHub
git pull origin main

echo "✅ Synced with GitHub"
